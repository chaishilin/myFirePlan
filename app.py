import streamlit as st
from utils import init_db, auto_backup_check

# ==========================================
# 1. 全局配置 (必须是第一行)
# ==========================================
st.set_page_config(
    page_title="个人资产管理系统",
    page_icon="💼",
    layout="wide"
)

# ==========================================
# 2. 系统初始化
# ==========================================
# 初始化数据库结构
init_db()

# 执行后台备份检查
auto_backup_check()

# ==========================================
# 3. 立即跳转
# ==========================================
# 不再显示任何欢迎语，直接切到 Dashboard。
# 如果用户没登录，Dashboard 页面内部的代码会检测到，并弹出侧边栏提示登录。
st.switch_page("pages/1_📊_资产看板.py")