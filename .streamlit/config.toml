[browser]
# 🚫 禁止收集使用统计 (这货是导致国内卡顿的罪魁祸首)
gatherUsageStats = false

[server]
# 🚫 禁止 CORS 检查 (防止某些局域网访问问题)
enableCORS = false
# 🚫 禁止 XSRF 保护 (防止跨域报错，局域网用没问题)
enableXsrfProtection = false
# 📍 固定端口 (可选)
port = 8501
headless = true